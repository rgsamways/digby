# Delta Spec: Points Economy

## Updates to openspec/specs/passport.md

Add a "Points Economy" section documenting:
- Points formula (stamp=25, unique mineral=10, hunt=100)
- `/api/passport/leaderboard` endpoint
- `total_points` and `hunt_completions` fields on `/me` response
